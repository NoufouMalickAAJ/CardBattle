// src/store/progressionSlice.js
import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const progressionSlice = createSlice({
  name: 'progression',
  initialState,
  reducers: {},
});

export default progressionSlice.reducer;